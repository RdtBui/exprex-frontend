import { useLocation } from 'react-router-dom'

const Header = () => {
    // Logo, Nav, div
    return (
        <header className='header container-full'>

        </header>
    )
}

export default Header;
